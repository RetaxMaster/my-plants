# Environments / secrets (stray copies at the workspace root)
.env
.env.*
!.env.example

# Node (stray local runs at root)
node_modules/
.nuxt/
.output/
dist/

# OS / editor
.DS_Store
*.swp
.idea/

# Local DB backups (sensitive — never commit)
database-backup/

# Playwright CLI scratch (local QA runs)
.playwright-cli/

# Claude Design import artifact (the raw prototype zip; tokens/components live in the app)
new-frontend.zip
